**Execução:**

```
python -m main.py
```

**Variáveis de modificação:**
```
vertices: representa um conjunto de labels que diferenciam os vértices do grafo
arestas: conjunto de tuplas que une dois vértices e estabelece uma taxa de diversidade
m: número de elementos desejado
```
